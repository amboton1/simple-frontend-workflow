const Header = () => {
    const template = `
        <header>
            <h1>Simple Workflow</h1>
            <p>Frontend workflow from scratch using parcel</p>
        </header>
    `
    return template;
}

export default Header;